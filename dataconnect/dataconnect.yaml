specVersion: "v1beta"
serviceId: "password_generator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "password_generator-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
